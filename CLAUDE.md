# プロジェクト概要

このプロジェクトはアフィリエイトブログサイト「お金の総合ガイド｜NEVORA」(GitHubリポジトリ: `nevora-money`)の企画・記事制作・SEO・サイト運営を行うためのフォルダです。サブエージェントを活用して記事コンテンツを制作し、Webサイト上でアフィリエイト広告を掲載することで収益化することを目的とします。

各サブエージェント・スキル定義(`.claude\agents\` `.claude\skills\`)では対象ジャンルを **[対象ジャンル]** というプレースホルダーのまま汎用的に記述している。これは今後ジャンルの幅を広げる際に個々のエージェント定義まで書き換えずに済ませるための意図的な設計であり、**このCLAUDE.mdの「対象分野」を対象ジャンルの唯一の情報源(Single Source of Truth)とする**。ジャンルを追加・変更する場合は、まずこのセクションを更新すること。

## 対象分野

- 現在の主軸: **投資・FX・税金(節税)**
- 現在のカテゴリ(`lib/categoryMeta.js`の`MAJOR_CATEGORIES`と一致させること): **投資 / FX / 税金・節税 / 保険 / 家計・節約 / クレカ・ポイント**
- 今後、上記に近接する関連ジャンルへ対象範囲を広げていく方針。新ジャンルを追加する際は、この一覧に追記し、カテゴリページ(`pages\category\[name].js`)や記事のfrontmatter`category`で個別記事ごとに分類する(サイト全体の構造やエージェント定義を都度作り直す必要はない)

## 目的

- 上記分野で検索需要のあるテーマ・キーワードを調査する
- 読者の役に立ち、検索エンジンで評価される記事を制作しサイトに公開する
- 記事内で関連商品・サービスを自然に紹介し、アフィリエイト収益につなげる
- アクセス解析・アフィリエイト成果をもとにサイトを継続的に改善する
- **Googleアドセンスの審査に通過し、広告掲載による収益化を実現する(最低条件)**

## アドセンス審査通過(最低条件)

サイト運営・記事制作の全工程において、Googleアドセンスの審査基準を満たすことを最優先の前提条件とする。以下を満たさないサイト・記事は公開・収益化の目的を達成できないため、各エージェントは自分の担当範囲でこれを常に意識すること。

- **十分な独自コンテンツ量**: 各記事は他サイトの要約・薄い内容ではなく、独自の情報・具体例・経験談を伴う一定量以上の本文を持つこと(目安として1記事あたり2000文字以上)
- **必須ページの整備**: プライバシーポリシー・運営者情報(お問い合わせ含む)・利用規約(必要な場合)がサイト全体のフッター等から常にアクセスできる状態であること。これらが揃っていないサイトは審査対象にすらならない
- **禁止コンテンツの排除**: 著作権侵害・アダルト・暴力的表現・危険行為の助長・虚偽情報など、アドセンスのプログラムポリシーで禁止されているコンテンツを含まないこと
- **金融YMYLへの配慮**: お金は「Your Money or Your Life」領域そのものであり、審査・評価が特に厳しい。利益や節税額を保証する表現、無登録の投資助言(個別銘柄の推奨)、税理士法に抵触しうる個別の税務相談への回答は行わない。リスク・手数料・適用条件・情報の時点を必ず併記する(詳細は法務チェックエージェントの定義を参照)
- **ナビゲーションのしやすさ**: サイト構造・メニューが整理されており、記事に迷わずたどり着けること(未完成ページ・ダミーリンク・準備中ページを公開状態にしない)
- **広告と紛らわしくない表示**: 自サイトの広告・アフィリエイトリンクが、通常のコンテンツやナビゲーションと明確に区別できる配置・表記になっていること
- 上記の詳細な確認観点は法務チェックエージェント・編集長エージェント・サイト制作者エージェントの各定義を参照する

## サイト構成

Next.js(React)+ GitHub + Vercel(無料ホスティング・自動デプロイ)を基本の技術スタックとする。記事はMarkdownファイルで管理し、将来的に必要になった段階でデータベースを導入する。予定しているサイト構成は以下の通り(詳細は `サイト運営\サイト本体\README.md` を参照)。

```
トップページ / 記事ページ / カテゴリページ / 比較ページ / ランキングページ / 検索機能 /
お悩み別ページ(/worry) / セルフ診断ページ(/diagnosis) /
管理者ダッシュボード(記事管理・広告管理・アクセス解析・SEO管理・AI自動更新) / データベース
```

## 作業方針

- リサーチ内容・記事・分析結果は、テーマごとにわかりやすいファイル名で保存する
- 出典元のURLや情報源など、後で裏取りできる情報を残す
- 記事は将来のジャンル分け(カテゴリページ)を見据え、Markdownのfrontmatterで`category`・`tags`等を管理できる形で執筆する
- 分析結果は簡潔にまとめ、根拠となるデータ・記事例を添える
- 記事制作・サイト運営に関する各種エージェント・スキルは `サイト運営` フォルダおよび `.claude\agents` `.claude\skills` を参照する

## デプロイ運用ルール

- デプロイはGitHubへの `git push` によるVercelの自動デプロイのみを正規の手段とする
- **`vercel` CLIからの手動デプロイ(`vercel --prod` 等)は行わないこと**。VercelプロジェクトのRoot Directory(`サイト運営\サイト本体`)配下にある `.gitignore`(`node_modules` `.next` を除外)は、CLIをリポジトリルートから実行した場合には適用されず、ローカルの `node_modules`(Windows環境でビルドされた実行権限のないバイナリを含む)がそのままアップロードされてビルドが `next: Permission denied`(exit 126)で失敗する不具合を2026-08-08に確認済み
- pushしてもVercelの自動デプロイが一定時間(目安10分程度)トリガーされない場合は、CLIでの代替デプロイを試みる前に、まず状況をユーザーに報告し指示を仰ぐこと
- **デプロイ状況・公開ページの確認は、pushから最低5分は待ってから行う**。Vercelのビルド完了(push→デプロイ反映)には3〜5分かかることがあるため、2分程度で「反映されていない」「トリガーされていない」と判断しない。5分経過してもデプロイが確認できない場合に初めて、上記の「一定時間(目安10分程度)トリガーされない」場合の報告フローに進む
- **push済みのコミットに対する `amend` + `force push` は行わないこと**。後から指定されたコミットメッセージが実際のコミットメッセージと異なっていても、変更内容そのものが正しければメッセージ不一致だけを理由に書き換えない。履歴の書き換えは、それによって失われるもの(共有履歴の一貫性・force pushのリスク)の方が大きいため、メッセージ相違は許容する

## デザイン上のコントラスト比の基準

- WCAG AAの4.5:1は最低ラインであり、これに近い値(目安として5:1未満)は実質的に不合格として扱う
- 新規に文字色・背景色を決める場合は6:1以上を目標とすること
- グラデーション背景の上に文字を置く場合、グラデーションの全範囲で基準を満たすか確認すること(両端だけでなく、最も条件の悪い箇所で判定する)
- 根拠: chart-summary-cardのリンク色が4.62:1(グラデーション片端でのAAぎりぎり)だったため、実画面で「背景と同化して見える」と複数回報告された(2026-08-09)。数値上AAを満たしていても、閾値ちょうどはマージン不足で実用上不十分になり得る

## エージェント定義の運用ルール

- エージェント定義は `.claude\agents\` 直下にフラットに置く。サブディレクトリ(特に日本語名)を作らない
- 改行コードはLFとする
- 定義を変更したら、必ずVS Codeを完全に再起動してから記事作業に入る
- git worktree が残っていると同名衝突で定義が読まれなくなる。エージェントが呼べない場合はまず `git worktree list` を確認する

## 骨組みだけ用意してある機能(中身は今後追加する)

美容サイトから複製した際に、ジャンル依存の中身は削除して枠組みだけ残した機能がある。いずれも**中身が未登録の間はページ自体を生成しない**(アドセンス審査基準「準備中ページを公開状態にしない」を満たすため)。

- **お悩み別ページ(`/worry/[slug]`)**: `サイト運営\サイト本体\lib\worryTopics.js`にチップ(お金の悩み12件)を定義済み。本文を`lib\worryContent.js`に同じslugで登録すると、その悩みのページ・トップのチップ・`/worry`ハブ・サイトマップに自動で載る
- **セルフ診断ページ(`/diagnosis/[slug]`)**: `lib\diagnosisTopics.js`に質問と結果を登録すると生成される。表示は`components\DiagnosisQuiz.js`が担当(データ駆動のためコンポーネント改修は不要)
- **写真素材(ヒーロー/セクションバンド/カテゴリカード)**: 美容サイトの画像は全削除済み。`サイト運営\サイト本体\scripts\generate-site-images.js`のMANIFESTに沿って元画像を`画像フォルダ\各種サイト\お金サイト\ホームページ修正用`に置き、`node scripts/generate-site-images.js`で生成する。未生成の間は画像を出さずグラデーション背景で代替する(`pages\index.js`の`hasHeroImage`等)
